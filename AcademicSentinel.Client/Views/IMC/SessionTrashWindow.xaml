<Window x:Class="AcademicSentinel.Client.Views.IMC.SessionTrashWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Trash — Past Sessions"
        Height="560" Width="940"
        WindowStartupLocation="CenterOwner"
        Background="#F6F8FA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1B5E20"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#E4E7EB"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#6B7280"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#C62828"/>

        <Style x:Key="OutlinedButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#101B5E20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Danger-coloured variant of OutlinedButton for the
             permanent-delete bulk action. Same shape and padding so
             the toolbar reads as one logical group. -->
        <Style x:Key="DangerOutlinedButton" TargetType="Button" BasedOn="{StaticResource OutlinedButton}">
            <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18"
                BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="TrashCanOutline" Width="22" Height="22"
                                                 Foreground="{StaticResource PrimaryBrush}" Margin="0,0,8,0"
                                                 VerticalAlignment="Center"/>
                        <TextBlock Text="Trash" FontSize="22" FontWeight="Bold" Foreground="#1B1B1B"/>
                    </StackPanel>
                    <TextBlock x:Name="TxtSubtitle"
                               Text="Sessions moved to Trash are permanently deleted after the retention period set by the server."
                               FontSize="12" Foreground="{StaticResource MutedBrush}"
                               TextWrapping="Wrap" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource OutlinedButton}"
                            Click="BtnRefresh_Click" Margin="0,0,8,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Refresh" Width="16" Height="16"
                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button Style="{StaticResource OutlinedButton}"
                            Click="BtnClose_Click">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Close" Width="16" Height="16"
                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Close" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="16"
                BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Bulk-action toolbar. Mirrors the layout of
                     RoomDetailWindow's "Select All + Delete Selected"
                     toolbar so the two surfaces feel consistent. -->
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <Button x:Name="BtnSelectAllTrash"
                            Style="{StaticResource OutlinedButton}"
                            Click="BtnSelectAllTrash_Click"
                            Padding="14,6">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="CheckboxMultipleMarkedOutline" Width="16" Height="16"
                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock x:Name="TxtSelectAllTrashLabel" Text="Select All" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnRestoreSelected"
                            Style="{StaticResource OutlinedButton}"
                            Click="BtnRestoreSelected_Click"
                            Margin="8,0,0,0" Padding="14,6">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Restore" Width="16" Height="16"
                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Restore Selected" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="BtnPurgeSelected"
                            Style="{StaticResource DangerOutlinedButton}"
                            Click="BtnPurgeSelected_Click"
                            Margin="8,0,0,0" Padding="14,6">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="TrashCanOutline" Width="16" Height="16"
                                                     VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Delete Permanently" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>

                <DataGrid x:Name="TrashGrid"
                          Grid.Row="1"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          RowHeight="36"
                          AlternatingRowBackground="#FAFBFC">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#1B5E20"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="12,0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Left"/>
                            <Setter Property="VerticalContentAlignment" Value="Center"/>
                            <Setter Property="BorderBrush" Value="#0E4514"/>
                            <Setter Property="BorderThickness" Value="0,0,1,0"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <!-- Selection checkbox column. Template column so
                             it works while the grid is read-only.
                             Mirrors the Past Session pattern. -->
                        <DataGridTemplateColumn Header="" Width="42">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Session ID"  Binding="{Binding SessionId}"        Width="110"/>
                        <DataGridTextColumn Header="Date"        Binding="{Binding DateDisplay}"      Width="180"/>
                        <DataGridTextColumn Header="Duration"    Binding="{Binding Duration}"         Width="100"/>
                        <DataGridTextColumn Header="Attendees"   Binding="{Binding AttendeesDisplay}" Width="100"/>
                        <DataGridTextColumn Header="Trashed At"  Binding="{Binding TrashedAtDisplay}" Width="180"/>
                        <DataGridTextColumn Header="In Trash"    Binding="{Binding InTrashDisplay}"   Width="*" MinWidth="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock x:Name="TxtEmptyState" Grid.Row="1"
                           Text="Trash is empty."
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="14"
                           HorizontalAlignment="Center" VerticalAlignment="Center"
                           Visibility="Collapsed"/>

                <TextBlock x:Name="TxtSummary" Grid.Row="2"
                           Margin="2,12,0,0"
                           Foreground="{StaticResource MutedBrush}"
                           FontSize="12"/>
            </Grid>
        </Border>
    </Grid>
</Window>
