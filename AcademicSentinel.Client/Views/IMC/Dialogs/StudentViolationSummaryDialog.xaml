<Window x:Class="AcademicSentinel.Client.Views.IMC.Dialogs.StudentViolationSummaryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AcademicSentinel.Client.Views.IMC.Dialogs"
        mc:Ignorable="d"
        Title="StudentViolationSummaryDialog"
        WindowStyle="None"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Width="520"
        Height="430"
        Background="Transparent">

    <Window.Resources>
        <SolidColorBrush x:Key="FEUGreenBrush" Color="#1B5E20" />
        <SolidColorBrush x:Key="FEUDarkTextBrush" Color="#1B1B1B" />
        <SolidColorBrush x:Key="FEUSubTextBrush" Color="#616161" />
        <SolidColorBrush x:Key="FEULightGrayBrush" Color="#F5F5F5" />

        <Style x:Key="FEUPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource FEUGreenBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="18,10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Background" Value="#2E7D32" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Margin="14"
            CornerRadius="8"
            Background="White"
            BorderBrush="#E0E0E0"
            BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.22" Color="Black" />
        </Border.Effect>

        <Grid Margin="18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="{Binding DialogTitle}"
                       FontSize="18"
                       FontWeight="Bold"
                       Foreground="{StaticResource FEUDarkTextBrush}"
                       Margin="0,0,0,14" />

            <Grid Grid.Row="1" Margin="0,0,0,14">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{StaticResource FEULightGrayBrush}" CornerRadius="8" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Total Violations" FontSize="12" Foreground="{StaticResource FEUSubTextBrush}" />
                        <TextBlock Text="{Binding TotalViolationsCount}" FontSize="26" FontWeight="Bold" Foreground="{StaticResource FEUDarkTextBrush}" />
                    </StackPanel>
                </Border>

                <!--
                    Risk Status card.
                    The risk label is wrapped + sized to 18 so the longest
                    wording ("Possible Dishonesty") fits in the half-width
                    column without truncation. The cumulative score moved
                    onto its own smaller line beneath the label, freeing
                    the headline from a "(NN pts)" suffix that previously
                    pushed it off-screen.
                -->
                <Border Grid.Column="2" Background="{StaticResource FEULightGrayBrush}" CornerRadius="8" Padding="14">
                    <StackPanel>
                        <TextBlock Text="Risk Status" FontSize="12" Foreground="{StaticResource FEUSubTextBrush}" />
                        <TextBlock Text="{Binding RiskLevelText}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{Binding RiskLevelColorHex}"
                                   TextWrapping="Wrap"
                                   LineHeight="22"
                                   LineStackingStrategy="BlockLineHeight"
                                   Margin="0,2,0,0" />
                        <TextBlock Text="{Binding RiskLevelScoreText}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{Binding RiskLevelColorHex}"
                                   Margin="0,2,0,0" />
                    </StackPanel>
                </Border>
            </Grid>

            <DataGrid Grid.Row="2"
                      Margin="0,0,0,14"
                      ItemsSource="{Binding ViolationSummaries}"
                      IsReadOnly="True"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      HeadersVisibility="Column"
                      RowHeaderWidth="0"
                      GridLinesVisibility="Horizontal"
                      BorderBrush="#E0E0E0"
                      BorderThickness="1"
                      AlternatingRowBackground="#FAFAFA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Violation Category" Binding="{Binding CategoryName}" Width="*" />
                    <DataGridTextColumn Header="Occurrences" Binding="{Binding Count}" Width="Auto" />
                    <DataGridTextColumn Header="Total Penalty Score" Binding="{Binding TotalScore}" Width="Auto" />
                </DataGrid.Columns>
            </DataGrid>

            <Button Grid.Row="3"
                    Content="Close"
                    Width="120"
                    HorizontalAlignment="Center"
                    Style="{StaticResource FEUPrimaryButton}"
                    Click="BtnClose_Click" />
        </Grid>
    </Border>
</Window>
