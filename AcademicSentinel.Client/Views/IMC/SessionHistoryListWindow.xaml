<Window x:Class="AcademicSentinel.Client.Views.IMC.SessionHistoryListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Session History"
        Height="620"
        Width="980"
        WindowStartupLocation="CenterOwner"
        Background="#F6F8FA"
        FontFamily="Segoe UI">

    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#1B5E20"/>
        <SolidColorBrush x:Key="SubtleBorderBrush" Color="#E4E7EB"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>

        <Style x:Key="MaterialDesignOutlinedButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="1.5"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#101B5E20"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MaterialDesignOutlinedButton}">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="18" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <materialDesign:PackIcon Kind="History" Width="22" Height="22" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,8,0"/>
                    <TextBlock Text="Session History" FontSize="22" FontWeight="Bold" Foreground="#1B1B1B"/>
                </StackPanel>

                <Button Grid.Column="1"
                        Content="Back"
                        Click="BtnBack_Click"
                        Style="{StaticResource MaterialDesignOutlinedButton}"/>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="{StaticResource CardBrush}" CornerRadius="12" Padding="16" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1">
            <Grid>
                <DataGrid x:Name="HistoryDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          MouseDoubleClick="HistoryDataGrid_MouseDoubleClick"
                          HeadersVisibility="Column"
                          RowHeaderWidth="0"
                          GridLinesVisibility="Horizontal"
                          BorderThickness="0"
                          AlternatingRowBackground="#FAFBFC">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Session ID" Binding="{Binding SessionId}" Width="1.0*"/>
                        <DataGridTextColumn Header="Start Time" Binding="{Binding StartTime, StringFormat={}{0:MMM dd, yyyy hh:mm tt}}" Width="2.1*"/>
                        <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" Width="1.1*"/>
                        <DataGridTextColumn Header="Attendees" Binding="{Binding AttendeeCount}" Width="1.0*"/>
                        <DataGridTextColumn Header="Violations" Binding="{Binding TotalViolations}" Width="1.0*"/>
                        <DataGridTemplateColumn Header="Action" Width="1.1*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="View"
                                            Click="BtnView_Click"
                                            Tag="{Binding}"
                                            Padding="10,5"
                                            HorizontalAlignment="Center"
                                            Style="{StaticResource MaterialDesignOutlinedButton}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
